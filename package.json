{
  "name": "issuetracker",
  "version": "1.0.0",
  "description": "To track buggs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -w server.js server.js",
    "compile": "babel src --presets react,es2015 --out-dir static"
     ,"compile": "babel src --presets react,es2015 --out-dir static -watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^4.13.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  }
}
